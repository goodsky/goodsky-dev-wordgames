{
  "name": "crossword",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run copy:dist",
    "copy:dist": "rm -rf ../../server/public/crossword && mkdir -p ../../server/public && cp -r dist ../../server/public/crossword",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.1",
    "svelte": "^5.43.8",
    "vite": "^7.2.4"
  }
}
